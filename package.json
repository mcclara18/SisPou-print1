{
  "name": "sispou",
  "version": "1.0.0",
  "description": "SISPOU - Sistema de Gestão de Hotel",
  "main": "back/config/electron.main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "start-back": "cd back && npm start",
    "start-front": "cd front && npm start",
    "dev": "concurrently \"npm run start-back\" \"npm run start-front\"",
    "test": "cd back && npm test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^38.4.0"
  },
  "dependencies": {
    "mysql2": "^3.15.3"
  }
}
